<h1>PostGenres#edit</h1>
<p>Find me in app/views/post_genres/edit.html.erb</p>


<div class="container">
  <div class="row　no-gutters">
    <div class="col-sm-12">

      <h2>ジャンル編集</h2>

      <% if @post_genre.errors.any? %>
        <h3><%= @post_genre.errors.count %>error prohibited this obj from being saved:</h3>
        <ul>
          <% @post_genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>

      <%= form_with model: @post_genre do |f| %>
        <div class="form-group">
          <%= f.label :投稿ジャンル名 %>
          <%= f.text_field :name, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.submit "完了", class: "btn btn-success", "data-disable-with": "Update User" %>
        </div>
      <% end %>

      <%= link_to 'Back', "/post_genres" %>

    </div>
  </div>
</div>

